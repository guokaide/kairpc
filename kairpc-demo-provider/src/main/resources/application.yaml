server:
  port: 8081

kairpc:
  zkServer: localhost:2181
  zkRoot: kairpc

app:
  id: app1
  namespace: public
  env: dev
  metas: "{dc: 'bj', gray: 'false', unit: 'B001'}"

logging:
  level:
    root: error
    com.kai.kairpc: info